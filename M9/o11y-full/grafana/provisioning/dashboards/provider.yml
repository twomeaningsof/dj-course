apiVersion: 1
providers:
  - name: 'Products App Dashboard'
    orgId: 1
    folder: ''
    type: file
    updateIntervalSeconds: 10
    allowUiUpdates: false  # Prevent manual changes from overriding
    options:
      path: /etc/grafana/provisioning/dashboards
